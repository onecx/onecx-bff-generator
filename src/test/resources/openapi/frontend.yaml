openapi: 3.0.3
info:
  title: Frontend API
  version: 1.0.0
paths:
  /users:
    get:
      operationId: getUsers
      tags:
        - Users
      responses:
        "200":
          description: ok
components:
  schemas:
    UserDto:
      type: object
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string


